import { Component, inject } from '@angular/core';
// import { ToastService } from '@org/shared/util-notification';

@Component({
  selector: 'lib-toast-container.component',
  imports: [],
  templateUrl: './toast-container.component.component.html',
  styleUrl: './toast-container.component.component.scss',
})
export class ToastContainerComponentComponent {
    // readonly _toastService = inject(ToastService);

}
